global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: comutitres-api
    metrics_path: /api/metrics
    static_configs:
      - targets:
          - backend:3000
        labels:
          application: comutitres
          environment: local
